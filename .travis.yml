sudo: required
services:
        - docker

before_install:
        - docker build -t hadyziade/docker-react -f Dockerfile.dev .

script:
        - docker run -e CI=true hady.ziade/docker-react npm run test
